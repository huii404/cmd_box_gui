<UserControl x:Class="CMD_BOX_GUI.UI.Views.UtilitiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="850">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="16">
            
            <!-- Khối 1: Auto Click & Spam Text -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Auto Click -->
                <Border Grid.Column="0" Style="{StaticResource CardPanel}" Margin="0,0,6,0" Padding="12">
                    <StackPanel>
                        <TextBlock Text="AUTO CLICK (ESC/F6 ngắt)" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,8"/>

                        <Grid Margin="0,0,0,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="TxtClickX" Text="500" Style="{StaticResource FlatTextBox}" Margin="0,0,4,0"/>
                            <TextBox x:Name="TxtClickY" Grid.Column="1" Text="500" Style="{StaticResource FlatTextBox}" Margin="4,0,0,0"/>
                        </Grid>

                        <Button x:Name="BtnGetCurrentMousePos" Content="Lấy vị trí chuột (Sau 3s)" Style="{StaticResource SecondaryButton}" Margin="0,0,0,6" Padding="8,4" FontSize="11" Click="BtnGetCurrentMousePos_Click"/>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Margin="0,0,4,0">
                                <TextBlock Text="Số lần" FontSize="10" Foreground="{DynamicResource TextMuted}"/>
                                <TextBox x:Name="TxtClickCount" Text="100" Style="{StaticResource FlatTextBox}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="4,0,0,0">
                                <TextBlock Text="Delay (ms)" FontSize="10" Foreground="{DynamicResource TextMuted}"/>
                                <TextBox x:Name="TxtClickDelay" Text="100" Style="{StaticResource FlatTextBox}"/>
                            </StackPanel>
                        </Grid>

                        <WrapPanel>
                            <Button x:Name="BtnStartAutoClick" Content="Bắt đầu Click" Style="{StaticResource PrimaryButton}" Margin="0,0,6,0" Padding="10,6" Click="BtnStartAutoClick_Click"/>
                            <Button x:Name="BtnStopAutoClick" Content="Dừng lại" Style="{StaticResource DangerButton}" Padding="10,6" Click="BtnStopAutoClick_Click"/>
                        </WrapPanel>
                    </StackPanel>
                </Border>

                <!-- Spam Text -->
                <Border Grid.Column="1" Style="{StaticResource CardPanel}" Margin="6,0,0,0" Padding="12">
                    <StackPanel>
                        <TextBlock Text="SPAM TEXT / AUTO PASTE (ESC/F6 ngắt)" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,8"/>
                        <TextBox x:Name="TxtSpamContent" Text="Xin chào bạn!" Style="{StaticResource FlatTextBox}" Margin="0,0,0,6"/>

                        <Grid Margin="0,0,0,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Margin="0,0,4,0">
                                <TextBlock Text="Số lần" FontSize="10" Foreground="{DynamicResource TextMuted}"/>
                                <TextBox x:Name="TxtSpamCount" Text="10" Style="{StaticResource FlatTextBox}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="4,0,0,0">
                                <TextBlock Text="Delay (ms)" FontSize="10" Foreground="{DynamicResource TextMuted}"/>
                                <TextBox x:Name="TxtSpamDelay" Text="200" Style="{StaticResource FlatTextBox}"/>
                            </StackPanel>
                        </Grid>

                        <WrapPanel>
                            <Button x:Name="BtnStartSpam" Content="Gửi Spam" Style="{StaticResource PrimaryButton}" Margin="0,0,6,0" Padding="10,6" Click="BtnStartSpam_Click"/>
                            <Button x:Name="BtnAutoPasteLines" Content="Paste nhiều dòng" Style="{StaticResource SecondaryButton}" Padding="10,6" Click="BtnAutoPasteLines_Click"/>
                        </WrapPanel>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Khối 2: Báo Cáo Pin Laptop (HTML) -->
            <Border Style="{StaticResource CardPanel}" Margin="0,0,0,12" Padding="12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="BÁO CÁO PIN LAPTOP (HTML)" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"/>
                        <TextBlock Text="Khởi tạo và xuất báo cáo chi tiết thông số, tình trạng và lịch sử pin laptop ra file HTML" FontSize="11" Foreground="{DynamicResource TextMuted}" Margin="0,2,0,0"/>
                    </StackPanel>
                    <Button Grid.Column="1" x:Name="BtnOpenBatteryHtml" Content="📄 Báo Cáo HTML" Style="{StaticResource PrimaryButton}" Padding="14,7" FontSize="11.5" Click="BtnOpenBatteryHtml_Click"/>
                </Grid>
            </Border>

            <!-- Khối 3: Gỡ Bloatware & Cài App Nhanh -->
            <Border Style="{StaticResource CardPanel}" Margin="0,0,0,12" Padding="12">
                <StackPanel>
                    <TextBlock Text="CÀI APP &amp; GỠ BLOATWARE" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,8"/>
                    <WrapPanel>
                        <Button x:Name="BtnUninstallBloatware" Content="Gỡ Bloatware Win" Style="{StaticResource DangerButton}" Margin="0,0,8,8" Padding="12,7" Click="BtnUninstallBloatware_Click"/>
                        <Button Content="Cài Chrome" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8" Padding="12,7" Click="BtnInstallChrome_Click"/>
                        <Button Content="Cài VS Code" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8" Padding="12,7" Click="BtnInstallVSCode_Click"/>
                        <Button Content="Cài Git" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8" Padding="12,7" Click="BtnInstallGit_Click"/>
                        <Button Content="Cài 7-Zip" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8" Padding="12,7" Click="BtnInstall7Zip_Click"/>
                    </WrapPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
