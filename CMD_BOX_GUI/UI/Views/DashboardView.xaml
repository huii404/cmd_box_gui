<UserControl x:Class="CMD_BOX_GUI.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="850">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="16">
            <!-- Header súc tích -->
            <TextBlock Text="BẢNG ĐIỀU KHIỂN" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,12"/>

            <!-- Grid Thống kê Tài nguyên Hệ thống (3 Thẻ ngang gọn gàng) -->
            <Grid Margin="0,0,0,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Quyền Hệ Thống -->
                <Border Grid.Column="0" Style="{StaticResource CardPanel}" Margin="0,0,6,0" Padding="12">
                    <StackPanel>
                        <TextBlock Text="QUYỀN HỆ THỐNG" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextMuted}" Margin="0,0,0,4"/>
                        <TextBlock x:Name="TxtAdminStatus" Text="..." FontSize="14" FontWeight="Bold" Foreground="{StaticResource AccentCyan}" Margin="0,0,0,8"/>
                        <Button x:Name="BtnElevateAdmin" Content="Cấp quyền Admin" Style="{StaticResource SecondaryButton}" Padding="8,4" FontSize="11" Click="BtnElevateAdmin_Click"/>
                    </StackPanel>
                </Border>

                <!-- Ổ Đĩa C: -->
                <Border Grid.Column="1" Style="{StaticResource CardPanel}" Margin="4,0,4,0" Padding="12">
                    <StackPanel>
                        <TextBlock Text="Ổ ĐĨA C:" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextMuted}" Margin="0,0,0,4"/>
                        <TextBlock x:Name="TxtDriveSpace" Text="..." FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,6"/>
                        <ProgressBar x:Name="PbDriveSpace" Style="{StaticResource FlatProgressBar}" Margin="0,0,0,4"/>
                        <TextBlock x:Name="TxtDriveDetail" Text="..." FontSize="10" Foreground="{StaticResource TextSecondary}"/>
                    </StackPanel>
                </Border>

                <!-- Nguồn & Pin -->
                <Border Grid.Column="2" Style="{StaticResource CardPanel}" Margin="6,0,0,0" Padding="12">
                    <StackPanel>
                        <TextBlock Text="NGUỒN &amp; PIN" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextMuted}" Margin="0,0,0,4"/>
                        <TextBlock x:Name="TxtPowerStatus" Text="..." FontSize="14" FontWeight="Bold" Foreground="{StaticResource AccentSuccess}" Margin="0,0,0,6"/>
                        <ProgressBar x:Name="PbBattery" Style="{StaticResource FlatProgressBar}" Foreground="{StaticResource AccentSuccess}" Margin="0,0,0,4"/>
                        <TextBlock x:Name="TxtBatteryDetail" Text="..." FontSize="10" Foreground="{StaticResource TextSecondary}"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Khối Nút Bấm Chữ Nhật Tối Giản -->
            <Border Style="{StaticResource CardPanel}" Margin="0,0,0,14" Padding="12">
                <StackPanel>
                    <TextBlock Text="THAO TÁC NHANH 1-CHẠM" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,10"/>
                    
                    <WrapPanel>
                        <Button x:Name="BtnQuickClean" Content="Xóa rác nhanh" Style="{StaticResource PrimaryButton}" Margin="0,0,8,8" Padding="12,7" Click="BtnQuickClean_Click"/>
                        <Button x:Name="BtnCleanPro" Content="Xóa rác PRO" Style="{StaticResource PrimaryButton}" Margin="0,0,8,8" Padding="12,7" Click="BtnCleanPro_Click"/>
                        <Button x:Name="BtnRepairNetwork" Content="Khôi phục Mạng" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8" Padding="12,7" Click="BtnRepairNetwork_Click"/>
                        <Button x:Name="BtnOptimizeTaskbar" Content="Tắt Taskbar W11" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8" Padding="12,7" Click="BtnOptimizeTaskbar_Click"/>
                        <Button x:Name="BtnOptimizeServices" Content="Tắt Dịch vụ ngầm" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8" Padding="12,7" Click="BtnOptimizeServices_Click"/>
                        <Button x:Name="BtnBatteryReport" Content="Chẩn đoán Pin" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8" Padding="12,7" Click="BtnBatteryReport_Click"/>
                    </WrapPanel>

                    <ProgressBar x:Name="PbActionProgress" Style="{StaticResource FlatProgressBar}" Margin="0,4,0,4" Visibility="Collapsed"/>
                    <TextBlock x:Name="TxtActionStatus" Text="" FontSize="11" Foreground="{StaticResource AccentCyan}" Visibility="Collapsed"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
