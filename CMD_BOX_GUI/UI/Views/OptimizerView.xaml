<UserControl x:Class="CMD_BOX_GUI.UI.Views.OptimizerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="850">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="16">
            <TextBlock Text="TỐI ƯU &amp; BẢO TRÌ HỆ THỐNG" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,12"/>

            <!-- Khối 1: Dọn Rác & Cache -->
            <Border Style="{StaticResource CardPanel}" Margin="0,0,0,12" Padding="12">
                <StackPanel>
                    <TextBlock Text="DỌN DẸP BỘ NHỚ &amp; CACHE" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,8"/>
                    <WrapPanel>
                        <Button x:Name="BtnCleanQuick" Content="Xóa rác nhanh" Style="{StaticResource PrimaryButton}" Margin="0,0,8,8" Padding="12,7" Click="BtnCleanQuick_Click"/>
                        <Button x:Name="BtnCleanPro" Content="Xóa rác PRO (Sâu)" Style="{StaticResource PrimaryButton}" Margin="0,0,8,8" Padding="12,7" Click="BtnCleanPro_Click"/>
                        <Button x:Name="BtnCleanDev" Content="Xóa Cache Dev" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8" Padding="12,7" Click="BtnCleanDev_Click"/>
                        <Button x:Name="BtnCleanBrowser" Content="Xóa Cache Browser" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8" Padding="12,7" Click="BtnCleanBrowser_Click"/>
                    </WrapPanel>
                </StackPanel>
            </Border>

            <!-- Khối 2: Tinh chỉnh Hệ thống & Khởi động -->
            <Border Style="{StaticResource CardPanel}" Margin="0,0,0,12" Padding="12">
                <StackPanel>
                    <TextBlock Text="TINH CHỈNH WINDOWS &amp; STARTUP" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,8"/>
                    <WrapPanel>
                        <Button x:Name="BtnDisableStartup" Content="Tắt App khởi động (Whitelist)" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8" Padding="12,7" Click="BtnDisableStartup_Click"/>
                        <Button x:Name="BtnOptimizeServices" Content="Tắt Dịch vụ ngầm (Telemetry/Xbox)" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8" Padding="12,7" Click="BtnOptimizeServices_Click"/>
                        <Button x:Name="BtnTaskbar" Content="Tắt Taskbar W11" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8" Padding="12,7" Click="BtnTaskbar_Click"/>
                        <Button x:Name="BtnSystemPro" Content="Tối ưu phản hồi PRO" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8" Padding="12,7" Click="BtnSystemPro_Click"/>
                    </WrapPanel>
                </StackPanel>
            </Border>

            <!-- Khối 3: Sửa lỗi hệ thống -->
            <Border Style="{StaticResource CardPanel}" Margin="0,0,0,12" Padding="12">
                <StackPanel>
                    <TextBlock Text="SỬA LỖI HỆ THỐNG" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,8"/>
                    <WrapPanel>
                        <Button x:Name="BtnFixWU" Content="Sửa Windows Update" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8" Padding="12,7" Click="BtnFixWU_Click"/>
                    </WrapPanel>
                </StackPanel>
            </Border>

            <ProgressBar x:Name="PbOptimizer" Style="{StaticResource FlatProgressBar}" Margin="0,2,0,4" Visibility="Collapsed"/>
            <TextBlock x:Name="TxtStatus" Text="" FontSize="11" Foreground="{StaticResource AccentCyan}" Visibility="Collapsed"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
